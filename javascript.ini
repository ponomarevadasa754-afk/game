javascript
// Геймплей: исправление багов ввода
function validateUserInput(input) {
    // Игрок должен дописать проверки
    if (input === "") return "ERROR: Empty input!";
    if (input.length < 3) return "ERROR: Too short!";
    // Постепенно добавляются новые правила
}
